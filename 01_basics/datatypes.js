"use strict"

/*
use strict enables strict mode in JavaScript. It prevents accidental errors like using 
undeclared variables and forces the code to follow modern, safer rules
*/




// number -> normal numbers, e.g., 100, 3.14
// bigint -> very large integers, e.g., 1234567890123456789n
// boolean -> true or false, e.g., true
// string -> text, e.g., "hello"
// null -> intentional empty value, e.g., null
// undefined -> variable not assigned, e.g., let a;
// symbol -> unique value, e.g., Symbol("id")

// typeof null object
// typeof undefined is undefined